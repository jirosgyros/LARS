# Copy to .env and fill in real values. .env is gitignored.
#
# No environment variables are currently required. The tailor pipeline
# shells out to the `claude` CLI (subscription-billed via Claude Code),
# so no Anthropic API key is needed.
#
# Add your own vars here if you extend the pipeline.
